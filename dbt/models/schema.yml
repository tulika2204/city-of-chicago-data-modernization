version: 2

sources:
  - name: city_ops_stg
    tables:
      - name: stg_permits
  - name: city_ops
    tables:
      - name: fact_inspections

models:
  - name: stg_permits
    columns:
      - name: permit_id
        tests:
          - not_null
      - name: permit_status
        tests:
          - not_null

  - name: int_permit_inspection_enriched
    columns:
      - name: permit_id
        tests:
          - not_null

  - name: mart_operational_risk
    columns:
      - name: community_area_id
        tests:
          - not_null
      - name: ward_id
        tests:
          - not_null
